<ui:FluentWindow x:Class="TeamsStatus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Teams Status Monitor" SizeToContent="WidthAndHeight" ResizeMode="NoResize"
        WindowBackdropType="Mica" ExtendsContentIntoTitleBar="True" WindowCornerPreference="Round"
        Closing="Window_Closing">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- System Tray Icon -->
        <tb:TaskbarIcon x:Name="MyNotifyIcon"
                        ToolTipText="Teams Status Monitor"
                        IconSource="pack://application:,,,/icon.ico"
                        TrayMouseDoubleClick="MyNotifyIcon_TrayMouseDoubleClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Öffnen" Click="MenuItem_Open_Click" />
                    <MenuItem Header="Status manuell setzen">
                        <MenuItem x:Name="MenuStatusAuto" Header="Auto (Teams)" Click="MenuItem_StatusAuto_Click" IsCheckable="True" IsChecked="True" />
                        <MenuItem x:Name="MenuStatusAvailable" Header="Verfügbar" Click="MenuItem_StatusAvailable_Click" IsCheckable="True" />
                        <MenuItem x:Name="MenuStatusBusy" Header="Beschäftigt" Click="MenuItem_StatusBusy_Click" IsCheckable="True" />
                        <MenuItem x:Name="MenuStatusAway" Header="Abwesend" Click="MenuItem_StatusAway_Click" IsCheckable="True" />
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="Log-Datei öffnen" Click="MenuItem_OpenLog_Click" />
                    <MenuItem Header="Nach Updates suchen..." Click="MenuItem_Update_Click" />
                    <MenuItem Header="Über Teams Status Monitor" Click="MenuItem_Info_Click" />
                    <Separator />
                    <MenuItem Header="Beenden" Click="MenuItem_Exit_Click" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

        <ui:TitleBar Title="Teams Status Monitor" Grid.Row="0" Margin="0,0,0,10" />

        <StackPanel Grid.Row="1">
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="100" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <ui:TextBlock Text="COM Port:" FontTypography="BodyStrong" VerticalAlignment="Center"/>
                        <Ellipse x:Name="ConnIcon" Width="10" Height="10" Fill="Red" Margin="10,0,5,0" VerticalAlignment="Center"/>
                        <ui:TextBlock x:Name="TxtConnStatus" Text="Getrennt" VerticalAlignment="Center" FontSize="11" Appearance="Secondary"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <ComboBox x:Name="CmbPorts" Width="130" SelectionChanged="CmbPorts_SelectionChanged"/>
                        <ui:Button x:Name="BtnConnect" Content="Verbinden" Icon="{ui:SymbolIcon Link24}" Click="BtnConnect_Click" Margin="10,0,0,0" Width="100"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <ui:TextBlock Text="Baudrate:" Margin="0,0,0,5" FontTypography="BodyStrong"/>
                    <ComboBox x:Name="CmbBaudRate" SelectionChanged="CmbBaudRate_SelectionChanged">
                        <ComboBoxItem Content="9600" IsSelected="True"/>
                        <ComboBoxItem Content="19200"/>
                        <ComboBoxItem Content="38400"/>
                        <ComboBoxItem Content="57600"/>
                        <ComboBoxItem Content="115200"/>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <ui:TextBlock Text="Helligkeit:" Margin="0,0,0,5" FontTypography="BodyStrong"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <Slider x:Name="SldBrightness" Width="320" Minimum="0" Maximum="255" Value="128" ValueChanged="SldBrightness_ValueChanged" VerticalAlignment="Center"/>
                <ui:TextBlock x:Name="TxtBrightness" Text="128" Margin="10,0,0,0" VerticalAlignment="Center" Width="30"/>
            </StackPanel>

            <ui:TextBlock Text="Modus:" Margin="0,0,0,5" FontTypography="BodyStrong"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <ui:Button x:Name="BtnModeAuto" Content="Auto (Teams)" Click="BtnMode_Click" Tag="Auto" Padding="8,4" Margin="0,0,5,0" />
                <ui:Button x:Name="BtnModeAvailable" Content="Verfügbar" Click="BtnMode_Click" Tag="A" Padding="8,4" Margin="0,0,5,0" />
                <ui:Button x:Name="BtnModeBusy" Content="Beschäftigt" Click="BtnMode_Click" Tag="B" Padding="8,4" Margin="0,0,5,0" />
                <ui:Button x:Name="BtnModeAway" Content="Abwesend" Click="BtnMode_Click" Tag="W" Padding="8,4" Margin="0,0,5,0" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20" VerticalAlignment="Center">
                <Ellipse x:Name="StatusIcon" Width="16" Height="16" Fill="Gray" Margin="0,0,8,0" Stroke="DarkGray" StrokeThickness="1"/>
                <ui:TextBlock x:Name="TxtStatus" Text="Status: Unbekannt" FontTypography="BodyStrong" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <ui:Button Content="Autostart installieren" Icon="{ui:SymbolIcon Add24}" Click="BtnInstallAutostart_Click" Margin="0,0,10,0" Padding="15,5"/>
                <ui:Button Content="Autostart deinstallieren" Icon="{ui:SymbolIcon Delete24}" Click="BtnUninstallAutostart_Click" Margin="0,0,10,0" Padding="15,5" Appearance="Danger"/>
                <ui:Button Content="Log öffnen" Icon="{ui:SymbolIcon Document24}" Click="MenuItem_OpenLog_Click" Padding="15,5" Margin="0,0,10,0"/>
                <ui:Button Content="Info" Icon="{ui:SymbolIcon Info24}" Click="MenuItem_Info_Click" Padding="15,5" Appearance="Primary"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
